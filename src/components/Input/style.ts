import styled from "styled-components";

export const StyledFieldset = styled.fieldset`

  > div > label {
    font-family: "Nunito";
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    color: #808080;
  }

  > div {
    border-radius: 8px;
  }

`;
